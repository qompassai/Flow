[Unit]
Description=Flow local capability snapshot (not an MCP daemon)

[Service]
Type=oneshot
ExecStart=%h/.local/bin/flow status --workspace %h/projects
WorkingDirectory=%h/projects
StandardOutput=journal
StandardError=journal
Environment=PYTHONUNBUFFERED=1

[Install]
WantedBy=default.target
